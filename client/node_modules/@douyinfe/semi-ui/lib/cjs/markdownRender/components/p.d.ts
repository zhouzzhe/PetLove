import * as React from 'react';
import { PropsWithChildren } from 'react';
import { TitleProps } from '../../typography';
declare const p: (props: PropsWithChildren<TitleProps>) => React.JSX.Element;
export default p;
